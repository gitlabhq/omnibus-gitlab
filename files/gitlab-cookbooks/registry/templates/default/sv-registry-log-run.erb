#!/bin/sh
exec chpst -P \
  -U root:<%= @options[:log_group] || 'root' %> \
  -u root:<%= @options[:log_group] || 'root' %> \
  svlogd <% unless @options[:log_format].eql?('json') %>-tt <% end %><%= @options[:log_directory] %>