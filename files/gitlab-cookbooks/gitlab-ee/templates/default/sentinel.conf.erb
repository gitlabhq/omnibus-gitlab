# This file is managed by gitlab-ctl. Manual changes will be
# erased! To change the contents below, edit /etc/gitlab/gitlab.rb
# and run `sudo gitlab-ctl reconfigure`.

port <%= @sentinel['port'] %>
sentinel monitor localhost <%= @redis['master_ip'] %> <%= @redis['master_port'] %> <%= @sentinel['quorum'] || 1 %>
sentinel down-after-milliseconds localhost 10000
sentinel failover-timeout localhost 60000
sentinel parallel-syncs localhost 1
