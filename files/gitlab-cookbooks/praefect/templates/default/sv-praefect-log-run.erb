#!/bin/sh
exec chpst -P \
  -U root:<%= @options[:log_group] || 'root' %> \
  -u root:<%= @options[:log_group] || 'root' %> \
  svlogd <% unless @options[:json_logging] %>-tt <% end %><%= @options[:log_directory] %>
