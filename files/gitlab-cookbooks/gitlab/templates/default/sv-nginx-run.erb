#!/bin/sh
exec 2>&1
<%= render("mount_point_check.erb") %>
cd <%= File.join(node['gitlab']['nginx']['dir'], "working") %>
exec chpst -P /opt/gitlab/embedded/sbin/nginx -c <%= File.join(node['gitlab']['nginx']['dir'], "etc", "nginx.conf") %>
