#!/bin/sh
exec 2>&1
<%= render("mount_point_check.erb") %>
exec /opt/gitlab/embedded/bin/remote_syslog --no-detach --debug-level DEBUG -c <%= @options[:dir] %>/remote_syslog.yml <%=  "--hostname #{@options[:logging_hostname]}" if @options[:logging_hostname] %>
