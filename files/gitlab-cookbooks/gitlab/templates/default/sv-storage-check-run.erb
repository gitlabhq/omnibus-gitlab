#!/bin/sh
set -e # fail on errors

cd <%= @options[:working_dir] %>

exec chpst -P \
-U <%= @options[:user] %>:<%= @options[:groupname] %> \
-u <%= @options[:user] %>:<%= @options[:groupname] %> \
<%= File.join(@options[:working_dir], 'bin', 'storage_check') %> -t <%= @options[:storage_check_target] %>
