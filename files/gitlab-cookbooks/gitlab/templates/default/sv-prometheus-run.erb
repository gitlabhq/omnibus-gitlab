#!/bin/sh
exec 2>&1
<%= render("mount_point_check.erb") %>
umask 077
exec chpst -P -U <%= node['gitlab']['prometheus']['username'] %> -u <%= node['gitlab']['prometheus']['username'] %> env KUBERNETES_SERVICE_HOST="${KUBERNETES_SERVICE_HOST}" KUBERNETES_SERVICE_PORT="${KUBERNETES_SERVICE_PORT}" \
  /opt/gitlab/embedded/bin/prometheus <%= @options[:flags] %>
